---
- name: nxos module
  hosts: nxos
  vars:
    ssh:
      host: "{{ ansible_host }}"
      username: admin
      password: Night$hade900
      transport: cli

      tasks:
         nxos_user:
           provider: "{{ ssh }}"
           - name: ansible
           state: present
           roles: network-admin
           configured_password: ansible
           update_password: on_create
